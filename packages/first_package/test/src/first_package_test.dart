// ignore_for_file: prefer_const_constructors

import 'package:first_package/first_package.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('FirstPackage', () {
    test('can be instantiated', () {
      expect(FirstPackage(), isNotNull);
    });
  });
}
